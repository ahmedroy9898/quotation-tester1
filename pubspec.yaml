name: almubarak_quotation
description: Al Mubarak Engineering — Construction Quotation Calculator (offline mobile app).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.2
  pdf: ^3.10.8
  printing: ^5.12.0
  share_plus: ^10.0.0
  path_provider: ^2.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
